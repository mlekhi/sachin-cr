document.addEventListener('DOMContentLoaded', () => {
  // Simple popup - just shows info
  // The main functionality is in the content script on Notion pages
  console.log('Sachin CR popup loaded');
});
